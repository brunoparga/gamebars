<%= render 'banner' %>
<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <div class="text-center" id="bar-count">
        We have <%= @bars.count %> game bars with <%= @games.count %> board games available!
      </div>
    </div>
  </div>
</div>
